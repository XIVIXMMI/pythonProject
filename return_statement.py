#return statement = Functions send Python values/object back to the caller
#                   these value/object are known as the function's return value

def multiplt(number1, number2):
    return number1 * number2


x = multiplt(5,6)

print(x)